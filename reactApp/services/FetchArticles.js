// FetchArticles.js

export const getArticles = () => {
    // let article_num = number;
    // const URL = "http://104.248.235.9:3000/api/${article_num}";
    // const URL = "http://192.168.1.11:3000/api/"; //use ifconfig for your ip
    // return fetch(URL).then((response) => response.json()).then((list) => {
    //     // once the fetch resolves, run the code here
    //     console.log(list);

    //     // not loading anymore, and we have some new data, the list of card objects we got from the json
    //     this.setState({ cards: list },
    //         function () { });

    // }).catch((error) => {
    //     console.log(error);
    // });
}